name: tcrpeg_env
channels:
  - defaults
  - conda-forge
  - bioconda
dependencies:
  - python
  - numpy
  - seaborn
  - matplotlib
  - tqdm
  - pandas
  - scikit-learn
  - scipy
  - snakemake
  - kneed
  - umap
  - plotly
  - pip
  - pip:
      - torch >= 1.1.0
#todo add version numbers to dependencies